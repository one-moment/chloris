# 모든 텍스트 파일을 LF로 정규화 (Windows ↔ Mac ↔ Linux 줄바꿈 충돌 방지)
* text=auto eol=lf

# 셸 스크립트는 반드시 LF (macOS/Linux에서 깨지지 않도록)
*.sh text eol=lf

# 바이너리 파일은 변환하지 않음
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.webp binary
*.woff  binary
*.woff2 binary
*.pdf  binary
*.zip  binary
